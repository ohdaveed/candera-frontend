interface:
  display_name: "Candera Frontend"
  short_description: "Repo-specific patterns and workflows for candera-frontend"
  default_prompt: "Use the candera-frontend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true